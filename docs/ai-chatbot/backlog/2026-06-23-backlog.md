---
title: AI 챗봇 남은 작업 백로그 (2026-06-23 재편성)
domain: ai-chatbot
type: report
status: in-progress
author: 이정헌
created: 2026-06-23
updated: 2026-06-23
related:
  - 2026-06-22-backlog.md
  - ../reports/2026-06-22-agent-mode-mvp-implementation.md
  - ../handoff/2026-06-22-handoff.md
---

# AI 챗봇 — 남은 작업 백로그 (2026-06-23 재편성)

`2026-06-22` 백로그를 기준으로 **2026-06-23 시점의 남은 작업을 재편성**한다.
완료 이력 전체는 [2026-06-22 백로그(동결 스냅샷)](2026-06-22-backlog.md) 및 그 이전 스냅샷을 참조한다.
이 문서가 이후 **남은 작업의 단일 living 출처**다.

## 현재 상태 (2026-06-23 시작)
- 3개 레포 모두 `master`·워킹트리 clean·동기화 완료(미푸시 0).
  - `no-home-backend` `e340baa` · `no-home-frontend` `d459dbb` · `no-home-artifact` `a91ffe1`.
- 직전 완료: **AI 에이전트(실행) 모드 MVP** 머지(backend [#16](https://github.com/repechage-team/no-home-backend/pull/16) · frontend [#5](https://github.com/repechage-team/no-home-frontend/pull/5)).
  액션 `search·setFilters·reset·clarify` + 필터 `sido·sigungu·startDealMonth·endDealMonth·aptName`.
- 질문 모드(`/api/ai/chat`)는 그대로 유지.

## 남은 작업

### P2 — 에이전트 Phase 2 (오늘의 핵심, 필터 먼저 → 액션)
[제안서](../proposals/2026-06-22-agent-mode.md) · [MVP 리포트](../reports/2026-06-22-agent-mode-mvp-implementation.md) · 구현 리포트(작성 예정 `../reports/2026-06-23-agent-mode-phase2-implementation.md`).
설계 원칙(MVP 계승): **capability = 단일 출처, 프론트가 최종 강제자.** 백엔드 프롬프트는 보조,
백엔드 가드는 레포 독립·안전 불변식(서울 지역·연월 범위·action allow-list)만 강제.

- ✅ **PR A — 필터** `sort · umdNm · minPrice · maxPrice` — **구현·검증 완료(미머지)**
  - 프론트 apply 경로 기존재(`filterSchema`·generic `applyAgentFilters`·`buildHouseSearchRequests`),
    백엔드 검색도 4필드 지원 → **백엔드 검색 코드 변경 0**. 백엔드는 `SYSTEM_PROMPT_TEMPLATE` 값 포맷 안내만.
  - 프론트 `buildAgentSummary`에 가격대·실효 정렬 표기 추가(UI 결합은 normalize 기존 처리로 정합).
  - **필터 값 가드 무추가**(프론트 최종 강제). 백엔드 `AgentCommandGuardsTest` passthrough 1건.
  - 검증: 백엔드 113 / 프론트 44 그린 + build. 상세 [Phase 2 리포트](../reports/2026-06-23-agent-mode-phase2-implementation.md).
- ✅ **PR B — 액션** `paginate · mapFocus · selectItem` — **구현·검증 완료(미머지)**
  - 예약 필드 `page · direction · itemIndex` 첫 사용. 백엔드 `ALLOWED_ACTIONS`+가드+프롬프트+`totalPages` 주입.
  - 프론트 신규 순수 모듈 `chat/agentActions.js`(`resolvePaginateTarget`·`resolveItemTarget`) + `handleAgentCommand` 3 case
    (기존 `searchHouses`/`selectItem`/`focusMapItem` 재사용).
  - **인덱싱**: 1-based → 프론트 0-based 변환. **상한**: 서버 하한만(page≥1·itemIndex≥1), 프론트 라이브 clamp.
  - 검증: 가드 7건·액션 13건 추가, 그린. mapFocus/selectItem 지도 표시는 브라우저 E2E 후속.
- ⬜ **(defer) 후속 소형 PR** — `isTimeout`/`isAuthFailure` 중복을 `AiProviderErrors`로 추출 ·
  `ai.agent.enabled` 독립 kill-switch. Phase 2 PR 비대화 방지를 위해 분리(기존 `chatClient==null→503`로 대체 가능).
- ⬜ **머지** — PR A/B 백엔드·프론트 (커밋·PR·머지는 사용자 지시 범위에서). 머지 순서: 백엔드 → 프론트.

### P1 — 안정성 / 품질
- **프론트 컴포넌트 테스트(Vitest)** — DOM/접근성/입력잠금. 팀 합의 후. [이슈](../../_shared/issues/2026-06-21-frontend-component-test-infra.md).
- **limiter 운영 확장** — Redis 분산 limiter, 사용자별 일일 한도, circuit breaker, 전체 요청 deadline, 429·지연·비용 지표/알림.

### P2 — 기능 확장 (Phase 2 외)
- **Phase 5 대화기억** — `MessageWindowChatMemory` + `MessageChatMemoryAdvisor` + JDBC, `conversationId=memberId`. 에이전트와 직교.
- **질의 능력 확대** — 가격대/면적/연식, 기간·추세, 다중 구 비교 / 비서울 lawdCd 매핑 확장(현재 서울 25개 자치구만).
- **스트리밍 응답(선택)** — SSE/fetch 스트림. 첫 질의 지연 UX 보강.

### 문서 / 팀 결정
- **Frontend·Artifact README** 현행화(팀 소유 영역).
- **문서 메타데이터 유지보수** — 인덱스·추천 진행 순서 최신화.

### 정리 (housekeeping)
- 워크스페이스 루트 scratch 스크립트 `e2e-{chat,queries,ratelimit,autoimport}.mjs` 4개(저장소 밖·미추적) — 유지/이동/삭제.
- full-stack 컨테이너(`Artifact`) — 유지 vs `docker compose down`.

### 머지 / 배포 전 확인
- **회귀 테스트** — 백엔드 전체·full-context 부팅·프론트 테스트/빌드·로그인/비로그인 챗봇 E2E.
- **키·비용·관측성** — 운영 키 주입 방식, 호출 비용 제한, 오류율/지연 로그.
- **ADR(선택)** — Spring AI 도입 + Boot 버전업을 `../_shared/adr/0001-*.md`로 기록.

## 추천 진행 순서
**보안 P0 완료 · AI MVP·문서 전부 master 머지 완료**(2026-06-22 백로그 참조).
남은 추천 순서: **에이전트 Phase 2 PR A(필터) → PR B(액션) → (defer) 후속 소형 PR** →
**P1**(프론트 컴포넌트 테스트(팀 합의) · limiter 운영 확장) →
**P2 기능**(대화기억 → 질의확대 → 스트리밍) → **문서/팀**(Frontend/Artifact README).
