<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.home.member.mapper.RefreshTokenMapper">

    <insert id="upsert">
        INSERT INTO member_refresh_tokens (member_id, token_hash, expires_at)
        VALUES (#{memberId}, #{tokenHash}, #{expiresAt})
        ON DUPLICATE KEY UPDATE
            token_hash = VALUES(token_hash),
            expires_at = VALUES(expires_at)
    </insert>

    <update id="rotate">
        UPDATE member_refresh_tokens
        SET token_hash = #{newTokenHash},
            expires_at = #{expiresAt}
        WHERE member_id = #{memberId}
          AND token_hash = #{currentTokenHash}
    </update>

    <delete id="deleteByTokenHash">
        DELETE FROM member_refresh_tokens
        WHERE token_hash = #{tokenHash}
    </delete>

    <delete id="deleteByMemberId">
        DELETE FROM member_refresh_tokens
        WHERE member_id = #{memberId}
    </delete>
</mapper>
